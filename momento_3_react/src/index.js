import React from 'react';//guardar etiquetas en variables
import ReactDOM from 'react-dom';//asignar etiqueta a la vista
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('app')
);